server:
  port: 9007
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
management:
  endpoints:
    jmx:
      exposure:
        include: '*'
